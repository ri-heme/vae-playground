defaults:
  - celeba_dataloader@train_dataloader
  - celeba_dataloader@valid_dataloader
  - celeba_dataloader@test_dataloader

train_dataloader:
  split: train
  shuffle: True

valid_dataloader:
  split: valid

test_dataloader:
  split: test
